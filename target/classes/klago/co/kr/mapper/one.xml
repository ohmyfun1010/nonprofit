<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.info.production.mapper.EquipmentMapper">
	
	<select id="test">
		select a.get_no
				,a.no
				,a.efe
		from ${DBERP}.abin a
	</select>
	
	<select id="test">
		select *
		from ${DBERP}.abin
		left join ${DBERP}.abin
	</select>
	
	<update id="test">
		update ${DBERP}.abin
	</update>
	
	<delete id="test">
		delete from ${DBERP}.abin
	</delete>
	
	<select id="test">
		select *
		from ${DBERP}.abin
	</select>
	
	<select id="test">
		select *
		from ${DBERP}.abin
	</select>
	
	<select id="test">
		select *
		from ${DBERP}.abin
	</select>
	
	<select id="test">
		select *
		from ${DBERP}.abin
	</select>

</mapper>
